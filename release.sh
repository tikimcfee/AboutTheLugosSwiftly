swift build -c release &&
cp ".build/release/Run" .

# rm -rf "release-package"
# mkdir "release-package"
# cp ".build/release/Run" "release-package/"
# cp "start-server.sh" "release-package/"
# cp -r "Public" "release-package/"
